// src/routes.js
// (Optional) Route list for future scalable navigation expansion
// (Not needed for current scope, but provided for structure as per instructions)

// TODO: Define application routes here as array of objects
// Each route object should include:
// - path: (string) The route URL
// - label: (string) The human-readable label for the route
// - showInMenu: (boolean) Whether or not to show in navigation menu
const routes = [
  // Example route object:
  // {
  //   path: '/',
  //   label: 'Home',
  //   showInMenu: false,
  // }
  // Add future routes as needed
];

// TODO: Export the routes array as default
// (e.g., export default routes;)
