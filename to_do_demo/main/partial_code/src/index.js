// src/index.js
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
// Import Bootstrap CSS
import 'bootstrap/dist/css/bootstrap.min.css';

// Application entry point
const root = ReactDOM.createRoot(document.getElementById('root'));
// TODO: Render your root React component for the application. 
// Use ReactDOM's root.render method to wrap your top-level <App /> component within <React.StrictMode> for highlighting potential problems in the app. 
// Example:
// root.render(
//   <React.StrictMode>
//     <App />
//   </React.StrictMode>
// );
// Make sure the application renders into the HTML element with ID 'root'.